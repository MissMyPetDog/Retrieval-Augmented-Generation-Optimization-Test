name: rag-opt
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - numpy>=1.24
  - scipy>=1.11
  - cython>=3.0
  - numba>=0.58
  - sentence-transformers>=2.2
  - datasets>=2.14
  - tqdm>=4.65
  - matplotlib>=3.7
  - pip:
      # Optional: install one that matches your CUDA runtime
      # - cupy-cuda12x>=12.0
      # - cupy-cuda11x>=12.0
      # Optional: distributed preprocessing
      # - pyspark>=3.5
